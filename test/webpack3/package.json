{
  "name": "test",
  "version": "1.0.0",
  "main": "../common/index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf build && rm -rf node_modules",
    "build": "yarn run clean && yarn && webpack",
    "test": "echo \"Testing webpack 3\" && yarn run build && node ../common/testRunner.js webpack3"
  },
  "dependencies": {
    "webpack": "^3.x.x"
  }
}
