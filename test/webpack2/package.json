{
  "name": "test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf build",
    "build": "yarn run clean && webpack",
    "test": "echo \"Testing webpack 2\" && yarn run build && node ./testRunner"
  },
  "dependencies": {
    "webpack": "^2.6.x",
    "webpack-cli": "^2.0.13"
  }
}
